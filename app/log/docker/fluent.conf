<source>
  type tail
  format ltsv
  tag editor.access-log
  path /var/log/app/editor.log
  pos_file /var/log/fluentd/editor.log.pos
</source>

<match editor.access-log>
  type websocket
  host editor-app   # default: 0.0.0.0 (ANY)
  port 80           # default: 8080
  use_msgpack false # default: false
  add_time false    # default: false
  add_tag true      # default: true
</match>

# <match /var/log/app/editor.log>
#   type copy
#   <store>
#     type file
#     path /var/log/fluentd/editor.log
#   </store>
#   <store>
#     type websocket
#     port 8080
#   </store>
# </match>
