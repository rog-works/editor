db:
  container_name: editor-db
#   image: localhost:49000/editor-db:latest
  image: redis:3.2.1-alpine
  volumes:
    - ~/editor/app/db/data:/data
  ports:
    - 6379:6379
  environment:
    USER: user
    PASSWORD: password
  command: redis-server --appendonly yes
